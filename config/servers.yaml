# Minecraft Server Configuration
# Define your Minecraft servers here

servers:
  # production:
  #   id: "prod_server_id"
  #   name: "Production Server"
  #   description: "Main survival server"
  #   backup_enabled: true
  #   backup_schedule: "0 2 * * *"  # Daily at 2 AM
  #   backup_type: "full"  # or "incremental"
    
  test:
    id: "fe08f84f"
    name: "test management system"
    description: "Minecraft Paper Server"
    backup_enabled: true
    backup_schedule: "0 2 * * 1-6"  # Mon-Sat at 2 AM (maintenance mode)
    backup_type: "full"

# Pterodactyl Configuration
pterodactyl:
  panel_url: "https://panel.destaria.com"
  api_key: "${PTERODACTYL_API_KEY}"  # Set via environment variable
